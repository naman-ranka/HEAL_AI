# Base URL of the HEAL.AI backend the frontend talks to.
# Leave unset for local dev (defaults to http://localhost:8000).
# Set this to your deployed backend when running the frontend against Railway, e.g.:
# VITE_API_BASE_URL=https://heal-backend-production.up.railway.app
# VITE_API_BASE_URL=
